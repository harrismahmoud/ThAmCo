@page
@model ThAmCo.Events.Pages.StaffList.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Staff[0].StaffName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff[0].Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Staff) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StaffName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-StaffId="@item.StaffId">Edit</a> |
                <a asp-page="./Details" asp-route-StaffId="@item.StaffId">Details</a> |
                    <a asp-page="./AddEvent" asp-route-StaffId="@item.StaffId">AddEvent</a> |
                <a asp-page="./Delete" asp-route-StaffId="@item.StaffId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
